class AppConstants {
  // App Name
  static const String appName = 'Color Match Trail';

  // General
  static const String appVersion = '1.0.0';
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
  static const Duration shortAnimationDuration = Duration(milliseconds: 150);
  static const Duration mediumAnimationDuration = Duration(milliseconds: 500);
  static const Duration longAnimationDuration = Duration(milliseconds: 1000);

  // Game Placeholders
  static const int startingLevel = 1;
  static const int startingCoins = 100;
  static const int maximumLives = 5;
  static const int defaultMoves = 20;

  // UI
  static const double standardPadding = 16.0;
  static const double smallSpacing = 8.0;
  static const double mediumSpacing = 16.0;
  static const double largeSpacing = 24.0;
  static const double standardRadius = 24.0;
}

